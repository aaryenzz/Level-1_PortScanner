# Level-1_PortScanner
A lightweight port scanner written in Python. This script scans a target IP address and identifies which ports are open by attempting TCP connections to each port in a specified range.
